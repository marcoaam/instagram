<% if @posts.any? %>
	<% @posts.each do |post| %>
		<%= post.title %>
		<%= image_tag post.picture.url(:medium), class: 'uploaded_pic' %>
	<% end %>
<% else %>
		No posts have been added	
<% end %>
<%= link_to('New post', new_post_path) %>